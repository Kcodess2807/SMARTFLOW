node_modules
backend/yolov8x.pt 
backend/yolov8l.pt 
backend/data/test3.mp4" 

# Python bytecode
*.pyc

# Weight files
# bin/

# Sample image data
sample_img/*.jpg
!sample_img/sample_*.jpg
sample_img/out/*

# Annotated test results
results/

# Intermediate training data
pyGame_simultions/darkflow/backup/
tfnet/yolo/parse-history.txt
tfnet/yolo/*.parsed
*.txt
!requirements*.txt
*.pb
/profile
/test.py

# Built cython files
pyGame_simultions/darkflow/cython_utils/*.pyd
pyGame_simultions/darkflow/cython_utils/*.c

#egg-info
darkflow.egg-info/

#Other build stuff
build/

#TensorBoard logs
summary/

#Built graphs
built_graph/

#Training checkpoints
ckpt/*

#pytest cache
.cache/
.env

# ---------------------------------------------------------------------------
# SmartFlow portfolio-refactor (backend RL + API)
# ---------------------------------------------------------------------------
# Python virtual environment
.venv/
# Training/eval artifacts: models, TensorBoard logs, plots, GIFs (regenerable)
backend/artifacts/
# SUMO intermediate trip file (the committed route file is derived from it)
backend/rl/nets/*.trips.xml
# Bytecode / caches
__pycache__/
*.pyc
# Published portfolio artifacts must be tracked despite the broad `results/`
# rule above (inherited from the old darkflow .gitignore).
!backend/results/
!backend/results/**

.venv/

# Local-only working notes (not for the shared repo)
AUDIT.md
